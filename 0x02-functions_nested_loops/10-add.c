#include "main.h"

/**
 * add- adds two integers, returns result
 * @m: to be added to n
 * @n: to be added to m
 * Return: sum of m and n
 */

int add(int m, int n)
{

	int sum;

	sum = m + n;
	return (sum);
}
