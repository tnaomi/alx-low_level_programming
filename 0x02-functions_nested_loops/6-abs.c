#include "main.h"

/**
 * _abs - displays an absolute val of n,
 * @n : the variable to be checked
 * Return: an absolute positivd integer
 */

int _abs(int n)
{

	if (n < 0)
	{
		return (-1 * n);
	}

	return (n);

}
