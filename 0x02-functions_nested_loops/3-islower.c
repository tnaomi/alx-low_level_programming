#include "main.h"

/**
 * _islower - checks whether 'c' is lowercase,
 * returns '1'-lowercase, '0'-uppercase
 */

int _islower(int c)
{
	int a = 97;

	if (c >= a)
	{
		return (1);
	}

	else
	{
		return (0);
	}

}
