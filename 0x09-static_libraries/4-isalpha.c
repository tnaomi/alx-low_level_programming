#include "main.h"

/**
* _isalpha - checks whether 'c' is alpha,
* @c : the variable to be checked
* Return: 1 if letter, 0 if otherwise
*/

int _isalpha(int c)
{
int a = 65;

if (c >= a)
{
return (1);
}

else
{
return (0);
}

}
